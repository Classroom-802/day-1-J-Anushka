# Input: An integer n
n = int(input("Enter a number: "))

# Counting the number of digits by converting the number to a string
digit_count = len(str(n))

# Output the result
print(digit_count)
